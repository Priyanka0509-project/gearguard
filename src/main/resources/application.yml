server:
  port: 8080

spring:
  application:
    name: gearguard-service

  # MongoDB Local Server Configuration
  data:
    mongodb:
      host: localhost
      port: 27017
      database: gearguard_db

  # Mail Server Configuration (Using Gmail as an example)
  # For local testing without a real mail server, you can use MailHog
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:workxyz05@gmail.com}
    password: ${MAIL_PASSWORD:bqlrwlirvtoafxjk}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  # Security / JWT Configuration (Custom keys)
  # You can reference these in your JwtService using @Value
security:
  jwt:
    secret-key: "G3arGuard_Secure_Authentication_Key_2025_Longer_Than_32_Chars"
    expiration: 36000000 # 10 hours in milliseconds

# Logging levels for easy tracking and debugging
logging:
  level:
    root: INFO
    com.example.gearguardUser: DEBUG
    org.springframework.data.mongodb: DEBUG
    org.springframework.security: INFO